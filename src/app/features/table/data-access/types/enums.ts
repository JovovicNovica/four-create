export enum ButtonTitles {
  ADD_USERS = 'Add User',
  CREATE_USERS = 'Create',
  CLOSE = 'X',
}

export enum TableTitles {
  ADD_USERS_TABLE_TITLE = 'List of users',
}

export enum InputErorrs {
  REQUIRED_FIELD = 'This field is required!',
  USER_EXISTS = 'Username already exists!',
}
